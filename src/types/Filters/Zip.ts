export interface Zip {
  id: number;
  code: string;
}

export interface ZipAPIProperties {
  id: number;
  fid: number;
  cp: string;
  cve_pais: string;
  cve_ent: string;
  cu_mun: string;
  c_p: number;
}