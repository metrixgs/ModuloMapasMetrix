export interface State {
  id: number;
  code: string;
  name: string;
}

export interface StateAPIProperties {
  id: number;
  fid: number;
  cve_ent: string;
  cve_pais: string;
  nom_ent: string;
}